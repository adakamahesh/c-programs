#include <stdio.h>
int main() {
    int i,n;//user input values
    printf("enter the i and n values");
    scanf("%d%d",&i,&n);//read user input values
    while(++i<=n)//while condition
    {
        
    }
    printf("final i value is%d ",i);//it is used to display final i value 
    return 0;
}
//from above condition ++i is pre increment of user input and check the condition 