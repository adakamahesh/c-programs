#include <stdio.h>
int main() {
    int i,j,n;//user input values
    printf("enter the i,j and n values");
    scanf("%d%d%d",&i,&j,&n);//read the user input values
    while(--i&&--j>=n)//while condition
    {
        
    }
    printf("final i value is%d\n",i);//it is used to display final i value
    printf("final j value is%d\n",j);//it is used to display final j value
    return 0;
}