#include <stdio.h>
int main() {
    int i,n;//user input values
    printf("enter the i and n valus");
    scanf("%d%d",&i,&n);//read the user input values
    while(i-->=n)//while condition 
    {
        
    }
    printf("final i valu is%d ",i);//it is used to display final i value
    return 0;
}
//from above condition i-- is post decrement operator on depends on user input value