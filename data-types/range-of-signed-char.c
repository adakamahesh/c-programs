#include <stdio.h>
#include<limits.h>//thes header file is used for limits of data types
int main()
{
    printf("signed char min :%d\n",SCHAR_MIN); //it is used for display min value
    printf("signed char max :%d\n",SCHAR_MAX); //it is used for display max value
    return 0;
}
