#include <stdio.h>
int main()
{
    int a,b;//it is user input values
    printf("enter the a and b values");
    scanf("%d%d",&a,&b);//it is used to read the user input valus
if(a==b)//it is condition whear the condition is true if block will be execute other wise else block will be execute  
{
    printf("hello\n");//it is used to display out put 
}else{
    printf("bayy");//it is used to display out put
}
    return 0;
}
