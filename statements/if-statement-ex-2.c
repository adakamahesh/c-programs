#include <stdio.h>
int main()
{
    if("mb")
    //it is condition whear conditon is true execute if block otherwise come to the out side of if block
{
    printf("Hello\nmahesh\n");//it is used to display the output
}
    return 0;
}
//from the above condition inside the string value accepts all characters and symboles except back space(\) 