#include <stdio.h>
int main()
{
    if(1)
    //it is condition whear conditon is true execute if block otherwise come to the out side of if block block  
{
    printf("Hello\nmahesh\n");//it is used to display the output
}
    return 0;
}
